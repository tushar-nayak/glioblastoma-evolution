# Results Notes

This folder is reserved for checked-in outputs from the runnable local
pipelines.

The repository currently includes:
- smoke-run visualizations for `patient_007` and `patient_067`
- loss curves generated by the history-prefix Neural ODE run

These files document that the pipelines can run and emit figures in the local
environment. They are not presented here as cohort-level performance claims.
